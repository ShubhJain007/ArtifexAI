[phases.install]
cmds = ["npm install -g corepack@0.24.1 && corepack enable", "pnpm i --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter web build"]

[start]
cmd = "npx serve build -l $PORT -s"
